import 'package:flutter/material.dart';

class Hompage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.teal,
      body: Column(
        children: [
          Center(child: Text("Heelooooo")),
        ],
      ),
    );
  }

}